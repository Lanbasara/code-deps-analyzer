{
  "private": true,
  "name": "client",
  "version": "0.0.0",
  "license": "MIT",
  "description": "Web page for code-deps-analyzer",
  "author": "Lanbasara <ghkagg@outlook.com>",
  "homepage": "https://github.com/Lanbasara/code-deps-analyzer#readme",
  "main": "lib/index.js",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "test": "jest"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^8.30.0",
    "eslint-config-preact": "^1.3.0",
    "preact-cli": "^3.4.2",
    "sirv-cli": "^2.0.2"
  },
  "dependencies": {
    "@mui/material": "^5.14.17",
    "preact": "^10.11.3",
    "preact-render-to-string": "^5.2.6",
    "preact-router": "^3.2.1",
    "react-d3-graph": "^2.6.0"
  }
}
